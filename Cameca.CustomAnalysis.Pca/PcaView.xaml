<UserControl x:Class="Cameca.CustomAnalysis.Pca.PcaView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:Cameca.CustomAnalysis.Pca"
             xmlns:utils="clr-namespace:Cameca.CustomAnalysis.Utilities.Controls;assembly=Cameca.CustomAnalysis.Utilities"
             xmlns:controls="clr-namespace:Cameca.Extensions.Controls;assembly=Cameca.Extensions.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Node}"
             d:DataContext="{d:DesignInstance Type={x:Type local:PrincipalComponentAnalysis}}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <utils:ButtonOverlayControl ButtonContent="Update"
                                ButtonCommand="{Binding UpdateCommand}"
                                OverlayVisibility="{Binding UpdateCommandCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">

        <TabControl>
            <TabItem Header="Noise Eigenvalues">
                <controls:Chart2D DataSource="{Binding NoiseEigenValues}"
                                  IsLogScaleY="True"
                                  AxisXLabel="Sorted eigenvalue index"
                                  AxisYLabel="Eigenvalue"/>
            </TabItem>

            <TabItem Header="Components">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateComponentsCommand}"
                               ButtonContent="Update"
                               OverlayVisibility="{Binding UpdateComponentsCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">

                    <local:ComponentsTileView ItemsSource="{Binding ComponentRenderData}" />
                </utils:ButtonOverlayControl>
            </TabItem>

            <TabItem Header="Grids">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateComponentsCommand}"
                               ButtonContent="Update"
                               OverlayVisibility="{Binding UpdateComponentsCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <local:ProjectionGridsView GridsSource="{Binding SelectedGridRenderData}" />

                </utils:ButtonOverlayControl>
            </TabItem>
            
            <TabItem Header="Loading">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateSelectedComponentCommand}"
                                ButtonContent="Update"
                                OverlayVisibility="{Binding UpdateSelectedComponentCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <lvc:CartesianChart Series="{Binding LoadingsSeries}"
                                        DisableAnimations="True"
                                        LegendLocation="None">
                        <lvc:CartesianChart.Resources>
                            <Style TargetType="lvc:DefaultTooltip">
                                <Setter Property="ShowSeries" Value="False" />
                            </Style>
                        </lvc:CartesianChart.Resources>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding LoadingsLabels}" FontSize="18">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Step="1" />
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Loadings" FontSize="18" LabelFormatter="{Binding AxisYLabelFormatter, Mode=OneTime}" />
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </utils:ButtonOverlayControl>
            </TabItem>

            <TabItem Header="Scores">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateSelectedComponentCommand}"
                            ButtonContent="Update"
                            OverlayVisibility="{Binding UpdateSelectedComponentCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <controls:Chart2D DataSource="{Binding ScoresHistogramData}"
                                  AxisXLabel="Score"
                                  AxisYLabel="Voxels"/>
                </utils:ButtonOverlayControl>
            </TabItem>
        </TabControl>
    </utils:ButtonOverlayControl>
</UserControl>
